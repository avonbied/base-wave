using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmbientEnvironment : MonoBehaviour
{
    // Start is called before the first frame update
    public AudioClip AmbientMainSounds;
    public AudioClip AmbientSub;
    public AudioSource AmbientAudioSource;

    //Use variables to play ambient sounds over time to create a varied environmental background audio.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
